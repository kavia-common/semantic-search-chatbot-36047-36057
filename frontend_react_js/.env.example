# PUBLIC_INTERFACE
# REACT_APP_BACKEND_URL is used by the frontend to reach the Django API.
# Do NOT include a trailing slash. Example values:
# http://localhost:8000/api
# https://your-domain.com/api
REACT_APP_BACKEND_URL=
